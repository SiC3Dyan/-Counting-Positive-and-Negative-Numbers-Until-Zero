#include <iostream>
using namespace std;

int main () {

  int num;
  int = positiveCount = 0;
  int = negativeCoount  = 0;

  while (true) 
  {

    if (num == 0) {
      break;
    } else if (num > 0) {
      positiveCount++;
    } else {
      negativeCount++;
    }
  }

  cout << "Positive Number entered." << positiveCount << endl;
  cout << "Negtive Number entered." << negativeCount << endl;

  return 0;
}
