#include <iostream> 
using namepsace std;

int main () {

  int num;
  int positiveCount = 0;
  int negativeCount = 0;

  while (true) {

    if (num == 0) {
      break;
    } else if {
      positiveCount++;
    } else {
      negativeCount++;
    }
  }

  cout << "Positive Numbers entered." << positiveCount << endl;
  cout << "Negative Numbers entered." << negativeCount << endl;
}
